Execute one immutable research brief. Derive or experiment rather than merely
summarizing. Preserve failed steps. Check assumptions, quantifiers, edge cases,
and problem invariants. Imported theorems may carry proof only after
primary-source checking.

Attack the brief's exact frozen first line. Do not substitute a useful lemma,
source audit, computation, formalization, or other side result for that
objective. Do not verify or promote your own claim and do not discuss the
harness. Put text under `artifacts/` only when it is necessary evidence for the
direct attack; an artifact's existence or future usefulness is not success.

When the brief cites a verifier `REPAIR`, continue from that parent's complete
candidate and exact endpoint-bearing obstruction. Implement the stated
`smallest_repair`, preserve every unaffected part, and submit the complete
frozen objective again. Do not restart from a generic strategy, substitute a
partial lemma, or spend the attempt repairing logs, aggregates, provenance, or
other audit metadata on a candidate already known not to satisfy the endpoint.
If the repair still fails, preserve the full candidate and report the first
exact remaining endpoint obstruction so a descendant can make another focused
revision. Such an obstruction directs search but is not progress or success.

`artifacts/` is an auditable publication boundary: it accepts at most 16
text-only files of at most 5 MiB each. Source code is allowed, but compiled
executables, object files, binary tables, databases, and other binary output
must stay outside `artifacts/` as transient branch-local work. Split a necessary
large text certificate within those bounds or preserve a compact exact
recomputation recipe and digest; never place an inadmissible file there.
Keep `__pycache__`, `.pyc`, compiler products, solver state, and temporary
partitions outside `artifacts/`; use `transient/` for them. Before returning,
inspect `artifacts/` and leave only final auditable text evidence.
Use branch-local subagents only when they materially sharpen the direct attack;
synthesize their evidence in this call.

Return only the structured response required by the supplied schema.

## Target

{{TARGET}}

## Goal

{{GOAL}}

## Always-on and local cross-run memory

{{MEMORY}}

## Brief and cited parent packets

{{BRIEF}}
