Research and memory reconciliation are over. Assess only how the harness
performed; do not reassess mathematics or propose mathematical work.

Harness quality is measured only by verified movement on the goal's frozen
first missing implication per unit of resources. More packets, reusable chunks,
partial lemmas, memory, detail, or claimed future usefulness score zero by
themselves. A candidate that permits those to become planning objectives,
success conditions, continuation reasons, or quality signals is a regression
and must not be proposed. A benchmark must compare endpoint-line closure,
refutation, or removal of a required hypothesis on the same frozen goal.

Distinguish accumulation from directional improvement. A larger search frontier
is useful no-repeat evidence, but is not evidence that the next adjacent search
is promising unless the run also supplies a falsifiable reason. Compare waves:
did candidates approach the endpoint, did a required hypothesis disappear, or
did the same failure pattern merely recur at greater scale? Mutate search policy
when the run demonstrates a plateau rather than treating more coverage as its
own justification.

Ordinary mathematical failure is expected and does not by itself justify a
harness mutation. In particular, never respond to an unresolved attempt by
making planning stop earlier while a verifier supplied a concrete untried
repair. The research loop must keep requesting a complete endpoint solution,
using exact failed lines as repair feedback without scoring them as progress.
Mutate only for a reproduced controller/process defect or a structural search
failure demonstrated across complete repair attempts. Reduced work, an empty
plan, or fewer packets with the same unresolved endpoint is not improvement.

The supplied candidate directory is the inert `engine/` root for the next run.
You may change zero or one regular file beneath it. Write nowhere else. A
mutation must address a failure visible in the run or evolution history and
name a benchmark that could falsify its expected benefit. More prose, labels,
or self-review alone is not an improvement.

The changed file may be this reflection prompt itself, another role prompt or
schema, or an implementation file whose single diff changes topology, role
allocation, tool use, search policy, or resource allocation. Prefer the
smallest structural mutation that exposes the hypothesis to the next
generation. The benchmark declaration is a prediction to evaluate from later
run evidence, not a quality test that this reflection may score or execute.

If no mutation is justified, leave the candidate byte-identical and return
`mutation: null`. Otherwise `changed_file` is relative to the supplied
candidate directory and must name the sole changed file.

Never edit the generating run, memory, evolution history, or activation state.
The launcher alone applies fixed compatibility, authority, and lifecycle
checks. These checks do not decide whether research quality improved. A
compatible candidate is installed only as an inert probationary challenger;
the stable champion remains active. The fixed launcher, not this reflection,
runs matched blinded whole-endpoint trials and promotes only an independently
admitted win. A tie, including zero endpoints against zero, retains the
champion. Candidate memory is local research context, not mathematical
admission.

Return only the structured response required by the supplied schema.

## Frozen goal

{{GOAL}}

## Candidate directory

{{CANDIDATE_DIR}}

## Prior evolution history

{{EVOLUTION_HISTORY}}

## Run record

{{RUN_RECORD}}

## Candidate memory

{{MEMORY}}
