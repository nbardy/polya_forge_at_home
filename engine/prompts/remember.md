Research is over. Emit only problem-local, evidence-linked candidate memory
changes that alter future work. Every change must cite audited `PASS` packets
and state its future use. Do not restate a mathematical claim; the packet is
the claim-bearing record.

Preserve failures by pointer. Do not copy transcript prose, raise claim
maturity, edit canonical files, or discuss the harness. Emit an empty changes
array when there is no reusable verified delta.

Return only the structured response required by the supplied schema.

## Goal

{{GOAL}}

## Frozen packets

{{PACKETS}}
