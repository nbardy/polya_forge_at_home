Research is over. Memory may point to audited direct results. A partial or
reusable byproduct may enter only as fallback salvage after its direct attack
failed or ended without completing the frozen line. Memory is never a research
objective, success condition, continuation reason, or harness-quality signal.
Emit only problem-local, evidence-linked changes that prevent future direct
attacks from repeating settled work. Every change must cite audited `PASS`
packets and state its future use. Use `preserve-salvage` only for a byproduct
that did not itself complete the frozen first line. Do not restate a
mathematical claim; the packet is the claim-bearing record.

Preserve failures by pointer. Do not copy transcript prose, raise claim
maturity, edit canonical files, or discuss the harness. Emit an empty changes
array when there is no verified fallback worth preserving. Memory volume and
future usefulness score zero.

Return only the structured response required by the supplied schema.

## Goal

{{GOAL}}

## Existing always-on and local cross-run memory

{{MEMORY}}

## Frozen packets

{{PACKETS}}
