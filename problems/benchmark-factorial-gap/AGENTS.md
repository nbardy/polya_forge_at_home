# Source-withheld benchmark rules

Prove the exact theorem in `target.md` from first principles. Do not search for
the historical solution or identify its catalog number during BUILD. Check all
uniform quantifiers, the balance and strict-window bounds, exact divisibility,
and the infinite-family step. This is a solved reproduction benchmark, not a
novelty target.
