# Blinded endpoint benchmark

This is deterministic harness-selection data, not a mathematical result.
Attack and verify the complete finite endpoint. Do not claim novelty or infer
research quality from partial-domain coverage, a hash prefix, or a near miss.
The plaintext solution must not be added to this problem pack.
Place the one exact candidate in `artifacts/endpoint.edn` using the schema in
`target.md`. Prose, including a correct token mentioned in prose, scores zero.
