{:format-version 1
 :id "find-token"
 :status :active
 :problem "blinded-endpoint-benchmark"
 :objective "Find and independently verify the unique token in PF-0000 through PF-9999 whose UTF-8 SHA-256 digest is a132c0c9c4ec6aae5bacf2ddb9d57a32cf6730aedfd27d44a0b5c28697cde1a6."
 :deliverable "The exact token in the required artifacts/endpoint.edn certificate and an independently reproducible exhaustive-domain verification."
 :endpoint-edge "The endpoint is closed by one exact domain token whose full SHA-256 digest matches and whose uniqueness is independently checked over all 10000 tokens."
 :first-open-line "There exists exactly one token PF-dddd with 0000<=dddd<=9999 whose exact UTF-8 SHA-256 digest is a132c0c9c4ec6aae5bacf2ddb9d57a32cf6730aedfd27d44a0b5c28697cde1a6."
 :inputs ["The literal finite token domain and full digest frozen in this pack"
          "Exact UTF-8 encoding and SHA-256 computation"
          "Deterministic exhaustive enumeration inside the assigned attempt directory"
          "The exact three-field EDN certificate schema frozen in target.md"]
 :exclusions ["Embedding or importing an external plaintext oracle"
              "Digest-prefix matches, guesses, or partial-domain searches presented as completion"
              "Builder self-verification, model agreement, or mathematical novelty claims"]
 :completion "A non-builder verifier identifies the exact structured certificate; the fixed launcher validates its exact schema, independently enumerates all 10000 tokens, finds exactly one full-digest match, and confirms byte equality."
 :kill "Stop when the finite invocation or wall-time budget is exhausted, the complete domain has been independently shown to contain no unique match, or no exact endpoint attack remains."
 :budget {:fanout 2 :invocations 12 :wall-minutes 10}}
