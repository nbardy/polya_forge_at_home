# Key learnings

No admitted problem-specific learnings yet.

The initial source audit records only these guardrails: `n=24` is not an
admissible witness because the target requires `n>24`; every `m<n` must be
checked; conditional arguments and finite negative searches do not resolve the
existential statement. Promote further mathematical content here only by
pointer to independently audited run evidence.
